__pycache__/
*.pyc
.venv/
.DS_Store

# 滾動 OHLC 視窗＝純計算快取（~7MB gzip，只給 build_ta 算指標、不給前端）。
# 不進 git：整包每天重寫會膨脹歷史（~2.5GB/年）。改走 GitHub Actions cache 跨日滾動；
# cache miss 時由 data/history_ohlc/（永久 archive 正本）用 --from-archive 秒重建（零網路）。
# 永久累積的正本是 data/history_ohlc/YYYY-MM-DD.json（append-only、每日一支 ~80KB、有進 git）。
data/ohlc_window.json.gz
